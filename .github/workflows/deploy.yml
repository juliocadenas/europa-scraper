name: Deploy to servidor01

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Deployment Script
        run: |
          cd $GITHUB_WORKSPACE
          bash scripts/deploy.sh
